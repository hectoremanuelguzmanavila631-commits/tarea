/* Imported in the HTML <head> with <link rel="stylesheet" href="styles.css"> */

body {
    font-family: 'Arial', sans-serif;
    margin: 0; /* Removes the browser's default margin */
    padding: 0;
    background-color: #f4f4f9;
    color: #333;
}

header {
    background-color: #2c3e50;
    color: white;
    padding: 20px;
    text-align: center;
}

nav ul {
    list-style: none;
    padding: 0;
}

nav li {
    display: inline;
    margin-right: 20px;
}
